// palindrome.cpp
#include <iostream>
#include <stdio.h>
using namespace std;
	
	int stringlength(char a[]){
		int length=0;
		for(int i=0;a[i]!='\0';i++){
			length++;
		}
		return length;
	}

	void checkpalindrome(ch)

int main(){
	char ch[100];
	gets(ch);
	int length = stringlength(ch);
	checkpalindrome(ch);
	return 0;
}